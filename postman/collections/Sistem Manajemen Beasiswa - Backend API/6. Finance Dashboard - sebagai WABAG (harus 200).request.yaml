$kind: http-request
url: "{{base_url}}/api/finance/dashboard"
method: GET
headers:
  Authorization: Bearer {{token_wabag}}
scripts:
  - type: afterResponse
    code: |-
      pm.test('Status 200', () => pm.response.to.have.status(200));
      pm.test('Response punya property data', () => pm.expect(pm.response.json()).to.have.property('data'));
    language: text/javascript
order: 6000
