$kind: http-request
url: "{{base_url}}/api/verifikasi/antrean"
method: GET
scripts:
  - type: afterResponse
    code: pm.test('Status 401 tanpa token', () => pm.response.to.have.status(401));
    language: text/javascript
order: 2000
