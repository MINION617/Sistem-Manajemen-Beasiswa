/* ============================================================
   config.local.example.js  — TEMPLATE (tracked in git)
   ============================================================
   Copy this file to `config.local.js` (same folder) and paste your
   real Supabase PUBLIC values. `config.local.js` is gitignored so the
   key never gets committed.

   The anon (publishable) key is safe for the browser — it is protected
   by Row Level Security. NEVER put the service-role key here.
   ============================================================ */
window.__SUPABASE_CONFIG__ = {
  url: 'https://YOUR_PROJECT.supabase.co',
  anonKey: 'YOUR_ANON_KEY',
};
