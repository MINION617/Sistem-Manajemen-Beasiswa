---
phase: 01-intake-foundation-and-program-setup
plan: 03
type: execute
wave: 3
depends_on: ["01-01"]
files_modified:
  - FRONTEND/STAFFADMIN/sponsorDanBeasiswa/manajemenSponsorBeasiswa.js
  - FRONTEND/STAFFADMIN/sponsorDanBeasiswa/manajemenSponsorBeasiswa.html
  - FRONTEND/STAFFADMIN/dashboardStaffAdmin.js
autonomous: true
requirements: [SPON-01, SCHL-01, AUTH-01]
user_setup: []
decisions:
  - "SUPERSEDED TASK: the original task to build FRONTEND/SPONSOR/sponsor.js is DROPPED — it is redundant with the existing 1109-line manajemenSponsorBeasiswa.js (audit C3). SPONSOR/sponsor.{html,js,css} (0-byte, unreferenced) are to be removed."
  - "KD-1 = Opsi A: sponsor/beasiswa CRUD persists to Supabase; the master data is the same source the mahasiswa scholarship list reads."
  - "KD-2: the admin surface is role 'staff' and routes from login.js to STAFFADMIN/dashboardStaffAdmin.html (NOT a separate SPONSOR page)."
must_haves:
  truths:
    - "Sponsor + beasiswa CRUD already exists in manajemenSponsorBeasiswa.js; this plan hardens it (persistence + role guard), it does not rebuild it."
    - "The staff admin master-data surface enforces an ACTIVE role guard (staff only) — currently the guard is commented out (audit M4)."
    - "Sponsor/beasiswa writes persist to Supabase and are reflected in the mahasiswa daftarBeasiswa list (single source of truth)."
    - "The dead SPONSOR/sponsor.{html,js,css} files are removed."
  artifacts:
    - path: "FRONTEND/STAFFADMIN/sponsorDanBeasiswa/manajemenSponsorBeasiswa.js"
      provides: "Hardened sponsor + beasiswa CRUD persisting to Supabase with active staff role guard"
      min_lines: 80
      contains: "role"
    - path: "FRONTEND/STAFFADMIN/dashboardStaffAdmin.js"
      provides: "Active staff role guard consumed from shared session"
      contains: "requireRole"
  key_links:
    - from: "FRONTEND/STAFFADMIN/sponsorDanBeasiswa/manajemenSponsorBeasiswa.js"
      to: "BACKEND/supabaseclient.js"
      via: "sponsor/beasiswa CRUD persistence"
      pattern: "sponsor|beasiswa|insert|update|delete"
    - from: "FRONTEND/STAFFADMIN/sponsorDanBeasiswa/manajemenSponsorBeasiswa.js"
      to: "FRONTEND/shared/session.js"
      via: "staff-only role guard"
      pattern: "requireRole|role|staff"
---

<objective>
Deliver the Phase 1 admin master-data slice by HARDENING the existing sponsor/beasiswa management, not by building a redundant new page.

Purpose: The audit found sponsor/beasiswa CRUD already fully implemented in manajemenSponsorBeasiswa.js (1109 lines) while the plan's target SPONSOR/sponsor.js is a dead 0-byte file. This plan satisfies SPON-01/SCHL-01 by wiring the existing surface to Supabase and turning on its role guard.
Output: persistent, role-guarded sponsor + beasiswa CRUD that feeds the mahasiswa scholarship list; dead files removed.
</objective>

<execution_context>
@$HOME/.codex/gsd-core/workflows/execute-plan.md
@$HOME/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/AUDIT-PHASE1.md
@.planning/phases/01-intake-foundation-and-program-setup/01-CONTEXT.md
@.planning/phases/01-intake-foundation-and-program-setup/01-01-SUMMARY.md
@FRONTEND/STAFFADMIN/sponsorDanBeasiswa/manajemenSponsorBeasiswa.js
@FRONTEND/STAFFADMIN/dashboardStaffAdmin.js
@FRONTEND/daftarBeasiswa/daftarBeasiswa.js
@BACKEND/supabaseclient.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Activate role guard + remove dead SPONSOR files</name>
  <files>FRONTEND/STAFFADMIN/dashboardStaffAdmin.js, FRONTEND/STAFFADMIN/sponsorDanBeasiswa/manajemenSponsorBeasiswa.js</files>
  <read_first>FRONTEND/STAFFADMIN/dashboardStaffAdmin.js, FRONTEND/STAFFADMIN/sponsorDanBeasiswa/manajemenSponsorBeasiswa.js, FRONTEND/shared/session.js</read_first>
  <action>Replace the commented-out role checks in the staff admin pages with an ACTIVE guard from `shared/session.js` (`requireRole('staff')`) that redirects non-staff to login. Keep a single DEV bypass flag if needed, but default to enforced. Delete the dead, unreferenced files `FRONTEND/SPONSOR/sponsor.html`, `sponsor.js`, `sponsor.css` (0-byte, linked from nowhere) via `git rm`.</action>
  <verify>grep -ni "requireRole\|session.role" FRONTEND/STAFFADMIN/dashboardStaffAdmin.js FRONTEND/STAFFADMIN/sponsorDanBeasiswa/manajemenSponsorBeasiswa.js; test ! -e FRONTEND/SPONSOR/sponsor.js</verify>
  <acceptance_criteria>
    - staff admin pages enforce an active staff-only role guard via shared session
    - non-staff sessions are redirected to login
    - FRONTEND/SPONSOR/sponsor.{html,js,css} are removed from the repo
  </acceptance_criteria>
  <done>The admin surface is genuinely access-controlled and the dead duplicate page is gone.</done>
</task>

<task type="auto">
  <name>Task 2: Persist sponsor + beasiswa CRUD to Supabase (single source of truth)</name>
  <files>FRONTEND/STAFFADMIN/sponsorDanBeasiswa/manajemenSponsorBeasiswa.js, FRONTEND/STAFFADMIN/sponsorDanBeasiswa/manajemenSponsorBeasiswa.html</files>
  <read_first>FRONTEND/STAFFADMIN/sponsorDanBeasiswa/manajemenSponsorBeasiswa.js, BACKEND/supabaseclient.js, FRONTEND/daftarBeasiswa/daftarBeasiswa.js</read_first>
  <action>Rewire the existing create/read/update/delete handlers (which currently mutate in-memory `dummySponsors`/`dummyBeasiswa` arrays) to persist through the Supabase client (sponsors + beasiswa tables, using shared/supabaseConfig.js or supabaseclient.js helpers). Preserve the existing UI, modals, validation, and cascade-delete behavior. Ensure beasiswa created/edited here are read by the mahasiswa `daftarBeasiswa` list (same backend, single source of truth) — column names must match the 01-04 schema (nama_program, nominal_dana, kategori, kuota, sponsor_id, status). Keep changes minimal and behavior-preserving aside from the persistence swap.</action>
  <verify>grep -ni "insert\|update\|delete\|from('sponsors')\|from('beasiswa')\|supabase" FRONTEND/STAFFADMIN/sponsorDanBeasiswa/manajemenSponsorBeasiswa.js</verify>
  <acceptance_criteria>
    - create/read/update/delete for sponsor AND beasiswa persist to Supabase (not in-memory arrays)
    - the mahasiswa scholarship list reflects beasiswa managed here
    - column/enum names match the 01-04 canonical schema
    - existing UI/modals/validation/cascade-delete behavior preserved
  </acceptance_criteria>
  <done>SPON-01 + SCHL-01 are satisfied by a persistent, role-guarded master-data surface that feeds the student catalog.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Non-staff sessions cannot reach the staff admin pages
- [ ] Create/edit/delete one sponsor and one beasiswa persist to Supabase
- [ ] A beasiswa created here appears in the mahasiswa daftarBeasiswa list
- [ ] FRONTEND/SPONSOR/sponsor.* no longer exist
- [ ] No verification/scoring/payout logic leaked into this Phase 1 surface
</verification>

<success_criteria>
- All tasks completed
- Sponsor + beasiswa CRUD is persistent, role-guarded, and the single source of truth for the catalog
- Redundant/dead files removed; no scope creep into later phases
</success_criteria>

<output>
After completion, create `.planning/phases/01-intake-foundation-and-program-setup/01-03-SUMMARY.md`
</output>
