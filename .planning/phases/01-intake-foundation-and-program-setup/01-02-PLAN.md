---
phase: 01-intake-foundation-and-program-setup
plan: 02
type: execute
wave: 3
depends_on: ["01-01"]
files_modified:
  - FRONTEND/profilMahasiswa/profilMahasiswa.js
  - FRONTEND/profilMahasiswa/profilMahasiswa.html
  - FRONTEND/daftarBeasiswa/daftarBeasiswa.js
  - FRONTEND/daftarBeasiswa/daftarBeasiswa.html
  - FRONTEND/pendaftaranSaya/pendaftaranSaya.js
autonomous: true
requirements: [PROF-01, DOCS-01, APPL-01, APPL-02]
user_setup: []
decisions:
  - "KD-1 = Opsi A: submission and document upload persist to Supabase (pendaftaran + dokumen_pendaftaran + storage), not to an in-memory Set."
  - "D-10 cutoff (from 01-01): an application is editable ONLY while status === 'menunggu_verifikasi'."
must_haves:
  truths:
    - "A mahasiswa MUST complete profile data AND core documents before the Daftar action is enabled (D-06/D-07) — this gate currently does not exist and must be built."
    - "Submitting an application creates a real `pendaftaran` record (status 'menunggu_verifikasi') and shows up in pendaftaranSaya — fixing the broken 1.5s fake-submit (audit C4)."
    - "A mahasiswa cannot submit twice to the same scholarship (anti-duplication)."
    - "Scholarship browsing keeps cards but prioritizes working search + filter (already present — preserve)."
    - "An application is editable only while pre-verification (status menunggu_verifikasi)."
  artifacts:
    - path: "FRONTEND/profilMahasiswa/profilMahasiswa.js"
      provides: "Profile completion + required-document upload + readiness state via shared rules"
      min_lines: 40
      contains: "isReadyToApply"
    - path: "FRONTEND/daftarBeasiswa/daftarBeasiswa.js"
      provides: "Gated submission that persists a real pendaftaran with canonical status"
      min_lines: 80
      contains: "menunggu_verifikasi"
    - path: "FRONTEND/pendaftaranSaya/pendaftaranSaya.js"
      provides: "Reads the same persisted applications so submit→tracking is connected"
      contains: "pendaftaran"
  key_links:
    - from: "FRONTEND/profilMahasiswa/profilMahasiswa.js"
      to: "FRONTEND/shared/applicationRules.js"
      via: "readiness calculation"
      pattern: "isProfileComplete|areCoreDocsComplete|isReadyToApply"
    - from: "FRONTEND/daftarBeasiswa/daftarBeasiswa.js"
      to: "BACKEND/supabaseclient.js"
      via: "createPendaftaran persistence"
      pattern: "createPendaftaran|pendaftaran|insert"
    - from: "FRONTEND/daftarBeasiswa/daftarBeasiswa.js"
      to: "FRONTEND/shared/applicationRules.js"
      via: "submit gating + editability checks"
      pattern: "isReadyToApply|isApplicationEditable"
---

<objective>
Deliver the mahasiswa Phase 1 vertical slice: a real readiness gate, a working application submission that persists, and a connected status view.

Purpose: The audit found the readiness gate missing entirely and the submit handler broken (fake delay, no record, no status). This plan builds the gate (D-06/D-07) and makes APPL-02 actually function end-to-end against the 01-04 backend.
Output: profilMahasiswa enforces readiness; daftarBeasiswa persists a real pendaftaran; pendaftaranSaya reflects it.
</objective>

<execution_context>
@$HOME/.codex/gsd-core/workflows/execute-plan.md
@$HOME/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/AUDIT-PHASE1.md
@.planning/phases/01-intake-foundation-and-program-setup/01-CONTEXT.md
@.planning/phases/01-intake-foundation-and-program-setup/01-01-SUMMARY.md
@FRONTEND/profilMahasiswa/profilMahasiswa.js
@FRONTEND/daftarBeasiswa/daftarBeasiswa.js
@FRONTEND/pendaftaranSaya/pendaftaranSaya.js
@BACKEND/supabaseclient.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the readiness gate on the mahasiswa profile flow</name>
  <files>FRONTEND/profilMahasiswa/profilMahasiswa.js, FRONTEND/profilMahasiswa/profilMahasiswa.html</files>
  <read_first>FRONTEND/profilMahasiswa/profilMahasiswa.js, FRONTEND/shared/applicationRules.js</read_first>
  <action>Add the readiness gate that the audit found missing. Use `shared/applicationRules.js` to compute profile completeness (nama, nim, program_studi, ipk, alamat, nomor_whatsapp) AND core-document completeness for the standard Phase 1 document package. Add real upload controls (`<input type=file>`) for each core document; on upload, persist to the `dokumen-pendaftar` storage bucket via the client (uploadDokumen/recordDokumen) and store metadata (jenis_dokumen, file name, uploaded_at, status). Surface explicit "what is still missing" feedback (which profile fields, which documents). Write profile updates through the client (updateProfile) and consume `shared/session.js` for the current user instead of redefining session logic.</action>
  <verify>grep -ni "isReadyToApply\|areCoreDocsComplete\|type=\"file\"\|uploadDokumen\|missing\|kurang" FRONTEND/profilMahasiswa/profilMahasiswa.js FRONTEND/profilMahasiswa/profilMahasiswa.html</verify>
  <acceptance_criteria>
    - profile page shows explicit missing-profile and missing-document feedback
    - core required documents can be uploaded and their metadata persisted (storage + dokumen_pendaftaran)
    - readiness is computed via shared rules and reflects profile + core docs
    - profile edits persist via the client, not just local form state
    - the page uses shared/session.js (no private getSession copy)
  </acceptance_criteria>
  <done>The profile flow is the authoritative readiness gate (D-06/D-07).</done>
</task>

<task type="auto">
  <name>Task 2: Fix submission to persist + connect to tracking</name>
  <files>FRONTEND/daftarBeasiswa/daftarBeasiswa.js, FRONTEND/daftarBeasiswa/daftarBeasiswa.html, FRONTEND/pendaftaranSaya/pendaftaranSaya.js</files>
  <read_first>FRONTEND/daftarBeasiswa/daftarBeasiswa.js, FRONTEND/pendaftaranSaya/pendaftaranSaya.js, FRONTEND/shared/applicationRules.js, BACKEND/supabaseclient.js</read_first>
  <action>Replace the fake 1.5s submit (audit C4) with a real flow: block the Daftar action unless `isReadyToApply(...)` passes, and when blocked show the exact reasons. On valid submit, call the client to create a `pendaftaran` row with status 'menunggu_verifikasi' (canonical, KD-4) — do NOT just add an id to an in-memory Set. Prevent duplicate submission to the same beasiswa. Make `pendaftaranSaya.js` read the same persisted applications so the new submission appears in tracking (closing the submit→status disconnect). Allow editing a submitted application only while `isApplicationEditable` is true (status menunggu_verifikasi). Keep the card UI and the existing working search/filter. Also parse the `?id=` query param to pre-select the scholarship in the detail modal (fixes audit N2).</action>
  <verify>grep -ni "isReadyToApply\|createPendaftaran\|menunggu_verifikasi\|isApplicationEditable\|duplicat\|sudahDaftar\|getParam\|searchParams" FRONTEND/daftarBeasiswa/daftarBeasiswa.js FRONTEND/pendaftaranSaya/pendaftaranSaya.js</verify>
  <acceptance_criteria>
    - submit is blocked with explicit reasons when not ready
    - a successful submit persists a pendaftaran with status 'menunggu_verifikasi'
    - the new application appears in pendaftaranSaya (submit→tracking connected)
    - duplicate submission to the same scholarship is prevented
    - editing is allowed only while status === 'menunggu_verifikasi'
    - cards + working search/filter preserved; ?id= deep-link pre-selects a scholarship
  </acceptance_criteria>
  <done>APPL-02 works end-to-end: gated submit → persisted record → visible status.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] With an incomplete profile/docs, Daftar is disabled and reasons are shown; after completing, the same student can submit
- [ ] A submit creates a real pendaftaran (status menunggu_verifikasi) visible in pendaftaranSaya
- [ ] Submitting again to the same scholarship is rejected
- [ ] Editing stops once status leaves menunggu_verifikasi
- [ ] Profile and application flows both use shared rules + session (no duplicated logic)
</verification>

<success_criteria>
- All tasks completed
- Readiness gate exists; submission persists with canonical status; tracking is connected
- Mahasiswa Phase 1 flow works end-to-end against the real backend
</success_criteria>

<output>
After completion, create `.planning/phases/01-intake-foundation-and-program-setup/01-02-SUMMARY.md`
</output>
