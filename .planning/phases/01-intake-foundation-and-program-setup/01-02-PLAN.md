---
phase: 01-intake-foundation-and-program-setup
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - FRONTEND/profilMahasiswa/profilMahasiswa.js
  - FRONTEND/profilMahasiswa/profilMahasiswa.html
  - FRONTEND/daftarBeasiswa/daftarBeasiswa.js
  - FRONTEND/daftarBeasiswa/daftarBeasiswa.html
  - FRONTEND/daftarBeasiswa/daftarBeasiswa.css
autonomous: true
requirements: [PROF-01, DOCS-01, APPL-01, APPL-02]
user_setup: []
must_haves:
  truths:
    - "A mahasiswa must complete profile data and core documents before applying to any scholarship (D-06, D-07)."
    - "Mahasiswa apply against one shared standard document package rather than per-beasiswa rules (D-08)."
    - "Mahasiswa can browse scholarships in a card-based interface with meaningful search and filter support (D-11)."
    - "A qualified mahasiswa can submit an application that starts in `Menunggu verifikasi` and remains editable only until verification starts (D-09, D-10)."
  artifacts:
    - path: "FRONTEND/profilMahasiswa/profilMahasiswa.js"
      provides: "Profile completion, required-document upload handling, and document-readiness state"
      min_lines: 40
    - path: "FRONTEND/daftarBeasiswa/daftarBeasiswa.js"
      provides: "Scholarship browsing and gated submission flow backed by shared rules"
      min_lines: 80
    - path: "FRONTEND/daftarBeasiswa/daftarBeasiswa.html"
      provides: "UI for scholarship discovery and application submission states"
      contains: "search"
  key_links:
    - from: "FRONTEND/profilMahasiswa/profilMahasiswa.js"
      to: "FRONTEND/shared/applicationRules.js"
      via: "readiness calculation"
      pattern: "complete|document|ready"
    - from: "FRONTEND/daftarBeasiswa/daftarBeasiswa.js"
      to: "FRONTEND/shared/dummyData.js"
      via: "scholarship and application state"
      pattern: "scholarship|application|profile"
    - from: "FRONTEND/daftarBeasiswa/daftarBeasiswa.js"
      to: "FRONTEND/shared/applicationRules.js"
      via: "submit gating and editability checks"
      pattern: "canSubmit|canEdit|ready"
---

<objective>
Deliver the mahasiswa Phase 1 vertical slice on top of the shared foundation.

Purpose: Make the student journey actually usable by enforcing the locked readiness and submission decisions (D-06 through D-11) and persisting them in the shared dummy state.
Output: Updated mahasiswa profile and scholarship pages that reflect the locked Phase 1 rules.
</objective>

<execution_context>
@$HOME/.codex/gsd-core/workflows/execute-plan.md
@$HOME/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-intake-foundation-and-program-setup/01-CONTEXT.md
@.planning/phases/01-intake-foundation-and-program-setup/01-RESEARCH.md
@.planning/phases/01-intake-foundation-and-program-setup/01-01-SUMMARY.md
@FRONTEND/profilMahasiswa/profilMahasiswa.js
@FRONTEND/daftarBeasiswa/daftarBeasiswa.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enforce readiness on the mahasiswa profile flow</name>
  <files>FRONTEND/profilMahasiswa/profilMahasiswa.js, FRONTEND/profilMahasiswa/profilMahasiswa.html</files>
  <read_first>FRONTEND/profilMahasiswa/profilMahasiswa.js, .planning/phases/01-intake-foundation-and-program-setup/01-CONTEXT.md</read_first>
  <action>Update the mahasiswa profile experience so it manages the required Phase 1 readiness fields using the shared dummy data modules. Add required document upload controls for the shared standard document package, using the browser file input or existing upload controls, and store uploaded document metadata such as document type, file name, upload timestamp, and dummy storage/status fields in the shared dummy state. Add visible state for what is still missing before the user can apply: incomplete profile fields and missing core documents. Make the page capable of writing both normalized profile readiness and uploaded document metadata back into the shared dummy source so the scholarship application page can trust it.</action>
  <verify>rg -n "ready|dokumen|profil|lengkap|missing|type=\"file\"|upload|fileName|uploadedAt|documents" FRONTEND/profilMahasiswa/profilMahasiswa.js FRONTEND/profilMahasiswa/profilMahasiswa.html</verify>
  <acceptance_criteria>
    - profile page exposes explicit missing-profile or missing-document feedback
    - profile page includes controls to attach/upload each core required document from the standard Phase 1 document package
    - uploaded document metadata is stored in shared dummy state and changes the document-readiness result
    - profile updates write to shared dummy state rather than only local form state
    - readiness logic matches the locked requirement that profile plus core docs are mandatory before applying
  </acceptance_criteria>
  <done>Mahasiswa profile flow becomes the authoritative readiness gate for Phase 1.</done>
</task>

<task type="auto">
  <name>Task 2: Gate scholarship submission and improve discovery utility</name>
  <files>FRONTEND/daftarBeasiswa/daftarBeasiswa.js, FRONTEND/daftarBeasiswa/daftarBeasiswa.html, FRONTEND/daftarBeasiswa/daftarBeasiswa.css</files>
  <read_first>FRONTEND/daftarBeasiswa/daftarBeasiswa.js, FRONTEND/beranda.js, FRONTEND/shared/applicationRules.js</read_first>
  <action>Refactor the scholarship list page to consume the shared scholarship/application state and rule helpers. Keep the card-based presentation, but prioritize practical discovery: search, filters, and clear status labels. Block submission when readiness is incomplete and explain exactly why. On successful submit, create or update the shared application record with initial status `Menunggu verifikasi`. Allow editing of a submitted application only while its shared verification state remains pre-verification.</action>
  <verify>rg -n "Menunggu verifikasi|filter|search|canSubmit|canEdit|ready" FRONTEND/daftarBeasiswa/daftarBeasiswa.js FRONTEND/daftarBeasiswa/daftarBeasiswa.html</verify>
  <acceptance_criteria>
    - scholarship page still uses cards and includes search/filter-first utility
    - submission flow writes initial status `Menunggu verifikasi`
    - users who are not ready see explicit blocking reasons instead of silent failure
    - editability rules stop once verification starts in shared state
  </acceptance_criteria>
  <done>Mahasiswa can discover scholarships and submit/edit applications under the locked Phase 1 rules.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `rg -n "Menunggu verifikasi|canSubmit|canEdit|ready|filter" FRONTEND/profilMahasiswa FRONTEND/daftarBeasiswa` returns expected hits
- [ ] `rg -n "type=\"file\"|upload|uploadedAt|documents" FRONTEND/profilMahasiswa FRONTEND/shared` returns expected hits for document upload behavior
- [ ] Browser smoke check: with missing documents, application submission is blocked with reasons; after attaching the required dummy documents, the same mahasiswa can submit
- [ ] Profile flow and application flow both consume the same readiness logic
- [ ] Scholarship browsing remains card-based while emphasizing discovery utility
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Mahasiswa Phase 1 flow works end-to-end against shared dummy data and respects the locked readiness and submission rules
</success_criteria>

<output>
After completion, create `.planning/phases/01-intake-foundation-and-program-setup/01-02-SUMMARY.md`
</output>
