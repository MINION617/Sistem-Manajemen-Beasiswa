---
phase: 01-intake-foundation-and-program-setup
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - .gitignore
  - BACKEND/.env.example
  - DATABASE/supabase_schema.sql
  - FRONTEND/shared/supabaseConfig.js
  - BACKEND/supabaseclient.js
autonomous: true
requirements: [AUTH-01, DOCS-01, SPON-01, SCHL-01]
user_setup:
  - "Create a Supabase project and paste real VITE_SUPABASE_URL + VITE_SUPABASE_ANON_KEY into BACKEND/.env (kept out of git)."
  - "Run DATABASE/supabase_schema.sql in the Supabase SQL editor to create tables, enums, RLS, and triggers."
  - "Create storage buckets 'dokumen-pendaftar' and 'bukti-transfer' (private) per the policies in the schema file."
decisions:
  - "KD-1 = Opsi A (Supabase nyata sekarang). This SUPERSEDES D-12 (JS dummy data first). Dummy data is downgraded to seed/fallback only."
  - "KD-4 canonical status vocabulary = snake_case (see schema enums); UI keeps a label map for display."
must_haves:
  truths:
    - "There is ONE canonical Supabase schema (DDL) that matches the table/column names used by BACKEND/supabaseclient.js (e.g. nama_program, status, ids as uuid)."
    - "Row Level Security is defined per role (mahasiswa, staff, kabag, wabag) so each role only reads/writes what it owns or is authorized for."
    - "Applicant documents and transfer proofs live in private storage buckets with per-owner path policies, not public arrays."
    - "Real Supabase credentials are loaded from a single source and never committed; BACKEND/.env is untracked and a .env.example documents the keys."
    - "Status values are canonical snake_case enums shared by schema and frontend (KD-4)."
  artifacts:
    - path: ".gitignore"
      provides: "Ignore BACKEND/.env, node_modules, OS/editor cruft"
      contains: ".env"
    - path: "BACKEND/.env.example"
      provides: "Documented placeholder env keys for Supabase"
      contains: "VITE_SUPABASE_URL"
    - path: "DATABASE/supabase_schema.sql"
      provides: "Canonical DDL: enums, 10 tables, FKs, RLS policies per role, storage bucket policies, profile-bootstrap trigger"
      min_lines: 120
      contains: "create policy"
    - path: "FRONTEND/shared/supabaseConfig.js"
      provides: "Single browser Supabase client/config consumed by all pages instead of 16 inline placeholder constants"
      min_lines: 20
      contains: "createClient"
  key_links:
    - from: "FRONTEND/shared/supabaseConfig.js"
      to: "BACKEND/.env.example"
      via: "documented config keys (URL + anon key)"
      pattern: "SUPABASE_URL|ANON_KEY"
    - from: "DATABASE/supabase_schema.sql"
      to: "BACKEND/supabaseclient.js"
      via: "table/column/enum names must match the client queries"
      pattern: "profiles|pendaftaran|nama_program|menunggu_verifikasi"
---

<objective>
Establish the REAL Supabase backend foundation for Phase 1 (Opsi A / KD-1), replacing the three non-functional data paths with one canonical schema, RLS, storage, and a single frontend client config — plus basic repo hygiene.

Purpose: Every requirement in every phase depends on a trustworthy, auditable data layer. The audit found none exists (no DDL/RLS/storage, .env committed, 16 duplicated placeholder configs). This plan creates that foundation so later slices wire to real data instead of dummy arrays.
Output: `.gitignore`, `BACKEND/.env.example`, `DATABASE/supabase_schema.sql` (canonical), and `FRONTEND/shared/supabaseConfig.js` (one client). `BACKEND/supabaseclient.js` reconciled to the schema.

NOTE: This plan SUPERSEDES D-12 (dummy-first). Dummy arrays remain only as optional seed data.
</objective>

<execution_context>
@$HOME/.codex/gsd-core/workflows/execute-plan.md
@$HOME/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/AUDIT-PHASE1.md
@.planning/phases/01-intake-foundation-and-program-setup/01-CONTEXT.md
@BACKEND/supabaseclient.js
@DATABASE/beasiswa.sql
@FRONTEND/LOGIN/login.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Repo hygiene — stop committing secrets</name>
  <files>.gitignore, BACKEND/.env.example</files>
  <read_first>BACKEND/.env</read_first>
  <action>Create a root `.gitignore` that ignores `BACKEND/.env`, `node_modules/`, and common OS/editor files. Run `git rm --cached BACKEND/.env` so the placeholder env stops being tracked (do NOT delete the local file). Create `BACKEND/.env.example` listing every key with placeholder values: VITE_SUPABASE_URL, VITE_SUPABASE_ANON_KEY, SUPABASE_SERVICE_ROLE_KEY, SUPABASE_JWT_SECRET, DATABASE_URL. Add a one-line comment that real values go in `.env` (gitignored).</action>
  <verify>test -f .gitignore && grep -q ".env" .gitignore && test -f BACKEND/.env.example && ! git ls-files --error-unmatch BACKEND/.env 2>/dev/null</verify>
  <acceptance_criteria>
    - `.gitignore` exists and ignores `BACKEND/.env` and `node_modules`
    - `BACKEND/.env` is no longer tracked by git (`git ls-files` does not list it)
    - `BACKEND/.env.example` documents all five keys with placeholder values
  </acceptance_criteria>
  <done>Secrets can no longer leak via git; config keys are documented.</done>
</task>

<task type="auto">
  <name>Task 2: Canonical Supabase schema (DDL) reconciled with the client</name>
  <files>DATABASE/supabase_schema.sql, BACKEND/supabaseclient.js</files>
  <read_first>BACKEND/supabaseclient.js, DATABASE/beasiswa.sql</read_first>
  <action>Write `DATABASE/supabase_schema.sql` as the single source of truth, reconciling the legacy MySQL dump with the names the client actually queries. Define: (1) postgres enums for canonical statuses (KD-4) — e.g. `status_pendaftaran` (menunggu_verifikasi, lolos_berkas, ditolak_berkas, wawancara, lolos_final, tidak_lolos_final), `status_penyaluran` (pending, sedang_diproses, sudah_cair), `status_laporan` (masuk, diproses, selesai), `app_role` (mahasiswa, staff, kabag, wabag). (2) Tables matching the client: `profiles` (id uuid PK ref auth.users, nim_nip unique, nama_lengkap, role app_role, program_studi, ipk, nomor_whatsapp, alamat), `sponsors`, `beasiswa` (sponsor_id FK, nama_program, nominal_dana, kategori, kuota, status), `pendaftaran` (mahasiswa_id FK profiles, beasiswa_id FK, status status_pendaftaran default menunggu_verifikasi, tanggal_daftar), `dokumen_pendaftaran`, `hasil_seleksi`, `penerima_beasiswa`, `penyaluran_dana`, `laporan_kendala`, `notifikasi`, with FKs and `created_at` timestamps. (3) A trigger that inserts a `profiles` row on `auth.users` signup (matches the "Profile auto-created via trigger" comment in the client). Then update `BACKEND/supabaseclient.js` ONLY where names diverge from this schema so queries resolve (do not change its behavior otherwise).</action>
  <verify>grep -ni "create table\|create type\|nama_program\|menunggu_verifikasi\|references auth.users" DATABASE/supabase_schema.sql</verify>
  <acceptance_criteria>
    - schema defines all 10 tables referenced by `supabaseclient.js` with matching column names
    - canonical status enums exist and match the snake_case values used across the prototype (KD-4)
    - foreign keys model the relations (profiles→pendaftaran→beasiswa→sponsors; pendaftaran→dokumen/hasil/penerima/penyaluran)
    - a profile-bootstrap trigger on auth.users signup is present
    - any column-name mismatches in `supabaseclient.js` are reconciled to the schema
  </acceptance_criteria>
  <done>One canonical, auditable data model exists and the client can resolve against it.</done>
</task>

<task type="auto">
  <name>Task 3: RLS policies per role + private storage buckets</name>
  <files>DATABASE/supabase_schema.sql</files>
  <read_first>DATABASE/supabase_schema.sql</read_first>
  <action>Extend the schema file with `alter table ... enable row level security` for every table and explicit `create policy` rules per role: mahasiswa can read/insert/update only their own `pendaftaran`/`dokumen_pendaftaran`/`profiles` and read public `beasiswa`/`sponsors`; staff can read all pendaftaran and write verification/scoring/payout rows; kabag can read monitoring/recipient data; wabag can read finance/payout data and verify transfer proof; only staff can write sponsor/beasiswa master data. Add storage bucket policy SQL/notes for two PRIVATE buckets: `dokumen-pendaftar` (path prefixed by the owner's user id — students write own docs, staff read) and `bukti-transfer` (staff write, wabag/kabag read). Use a `role()` helper reading the profile role (or auth.jwt claim) consistently.</action>
  <verify>grep -ci "create policy" DATABASE/supabase_schema.sql; grep -ni "row level security\|dokumen-pendaftar\|bukti-transfer" DATABASE/supabase_schema.sql</verify>
  <acceptance_criteria>
    - RLS is enabled on every table
    - per-role policies exist for mahasiswa, staff, kabag, wabag (least-privilege)
    - sponsor/beasiswa writes are restricted to staff
    - two private storage buckets with per-owner path policies are documented in SQL/comments
  </acceptance_criteria>
  <done>Data access is least-privilege per role and uploads are owner-scoped (addresses the upload-security pitfall).</done>
</task>

<task type="auto">
  <name>Task 4: One shared frontend Supabase client</name>
  <files>FRONTEND/shared/supabaseConfig.js</files>
  <read_first>FRONTEND/LOGIN/login.js, BACKEND/supabaseclient.js</read_first>
  <action>Create `FRONTEND/shared/supabaseConfig.js` that exports a single configured Supabase client for the browser (esm.sh CDN import of @supabase/supabase-js@2), reading URL + anon key from a small documented config (since there is no bundler, expose a `window.__SUPABASE_CONFIG__` or a single editable constants block — NOT 16 duplicated placeholders). Document that pages should import/consume this instead of redeclaring `const SUPABASE_URL='https://YOUR_PROJECT.supabase.co'`. Do NOT rewrite all 16 pages here — that wiring happens in 01-01 (auth/core) and 01-02/01-03 (per-slice). Just provide the canonical module and a usage comment.</action>
  <verify>grep -ni "createClient\|export" FRONTEND/shared/supabaseConfig.js</verify>
  <acceptance_criteria>
    - `FRONTEND/shared/supabaseConfig.js` exports one configured client
    - it reads config from a single place (no per-page placeholder duplication in this module)
    - includes a comment instructing pages to consume it instead of inline constants
  </acceptance_criteria>
  <done>There is one place to configure Supabase for the browser; later plans wire pages to it.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `git ls-files | grep BACKEND/.env` returns nothing; `.gitignore` and `BACKEND/.env.example` exist
- [ ] `DATABASE/supabase_schema.sql` defines 10 tables, status enums, FKs, a profile trigger, RLS on all tables, per-role policies, and storage bucket policies
- [ ] Table/column/enum names in the schema match the queries in `BACKEND/supabaseclient.js`
- [ ] `FRONTEND/shared/supabaseConfig.js` exports one client with single-source config
- [ ] No real secret values are committed
</verification>

<success_criteria>
- All tasks completed
- Canonical schema + RLS + storage foundation exists and is reconciled with the client
- Secrets are no longer tracked; config is documented
- The project has a real, auditable data layer for later slices to wire to
</success_criteria>

<output>
After completion, create `.planning/phases/01-intake-foundation-and-program-setup/01-04-SUMMARY.md`
</output>
