---
phase: 01-intake-foundation-and-program-setup
plan: 01
type: execute
wave: 2
depends_on: ["01-04"]
files_modified:
  - FRONTEND/shared/session.js
  - FRONTEND/shared/dummyData.js
  - FRONTEND/shared/applicationRules.js
  - FRONTEND/LOGIN/login.js
  - FRONTEND/LOGIN/login.html
autonomous: true
requirements: [AUTH-01, PROF-01, DOCS-01, APPL-01, APPL-02, SPON-01, SCHL-01]
user_setup: []
decisions:
  - "KD-1 = Opsi A: wire login to REAL Supabase Auth (AUTH-01). Dummy accounts kept only as an explicit dev fallback behind a flag, not the primary path."
  - "KD-2: role model is {mahasiswa, staff, kabag, wabag} (+ kabag_wabag login mode) — NOT a binary mahasiswa/admin."
  - "REBASELINE: login.js is ALREADY a full 4-role router (704 lines). This plan must CONSUME it, not rewrite its routing."
must_haves:
  truths:
    - "login.js stays a 4-role router; all four redirects (mahasiswa→dashboard, staff→dashboardStaffAdmin, kabag→dashboardKabag, wabag→dashboardWabag) keep working (regression guard)."
    - "Session helpers are centralized in FRONTEND/shared/session.js around the existing `bk_user` contract used by every page, not a new contract."
    - "DUMMY_ACCOUNTS and other page-local seed arrays are consolidated into FRONTEND/shared/dummyData.js to kill duplication (DRY)."
    - "Application/readiness rules and the canonical snake_case status vocabulary (KD-4) live in FRONTEND/shared/applicationRules.js so later slices reuse them."
    - "Login authenticates against real Supabase Auth via FRONTEND/shared/supabaseConfig.js (from 01-04); dummy login is an explicit, clearly-flagged dev fallback."
  artifacts:
    - path: "FRONTEND/shared/session.js"
      provides: "Shared getSession/saveSession/clearSession + role-aware redirect, generalized from existing bk_user usage"
      min_lines: 30
      contains: "bk_user"
    - path: "FRONTEND/shared/dummyData.js"
      provides: "Single seed source for accounts, sponsors, scholarships, profiles, applications (consolidated from page-local arrays)"
      min_lines: 60
    - path: "FRONTEND/shared/applicationRules.js"
      provides: "Canonical status enum + readiness/editability helpers"
      min_lines: 30
      contains: "menunggu_verifikasi"
    - path: "FRONTEND/LOGIN/login.js"
      provides: "Login wired to shared session + real Supabase auth, dummy fallback flagged"
      contains: "role"
    - path: "FRONTEND/LOGIN/login.html"
      provides: "Loads shared session/config modules"
      contains: "script"
  key_links:
    - from: "FRONTEND/LOGIN/login.js"
      to: "FRONTEND/shared/supabaseConfig.js"
      via: "real Supabase auth client"
      pattern: "supabase|signIn|auth"
    - from: "FRONTEND/LOGIN/login.js"
      to: "FRONTEND/shared/session.js"
      via: "session persistence helpers"
      pattern: "saveSession|bk_user"
    - from: "FRONTEND/shared/applicationRules.js"
      to: "FRONTEND/shared/dummyData.js"
      via: "readiness evaluation against shared profile/document state"
      pattern: "profile|document|application"
---

<objective>
Centralize the Phase 1 frontend foundation — shared session, consolidated seed data, and canonical application rules — and wire login to REAL Supabase Auth while preserving the existing 4-role router.

Purpose: Eliminate the duplicated per-page session/dummy logic (the DRY/divergence problem found in the audit) and connect authentication to the real backend from 01-04, without regressing the working role routing in login.js.
Output: FRONTEND/shared/{session,dummyData,applicationRules}.js and a login flow consuming them + Supabase Auth.
</objective>

<execution_context>
@$HOME/.codex/gsd-core/workflows/execute-plan.md
@$HOME/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/AUDIT-PHASE1.md
@.planning/phases/01-intake-foundation-and-program-setup/01-CONTEXT.md
@.planning/phases/01-intake-foundation-and-program-setup/01-04-PLAN.md
@FRONTEND/LOGIN/login.js
@FRONTEND/LOGIN/login.html
@FRONTEND/daftarBeasiswa/daftarBeasiswa.js
@FRONTEND/profilMahasiswa/profilMahasiswa.js
@BACKEND/supabaseclient.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create shared session, seed data, and rule modules</name>
  <files>FRONTEND/shared/session.js, FRONTEND/shared/dummyData.js, FRONTEND/shared/applicationRules.js</files>
  <read_first>FRONTEND/LOGIN/login.js, FRONTEND/dashboard/dashboard.js, FRONTEND/pendaftaranSaya/pendaftaranSaya.js, FRONTEND/profilMahasiswa/profilMahasiswa.js</read_first>
  <action>Extract the `getSession()` / `saveSession()` / logout logic that is currently copy-pasted across ~11 pages (keyed on `bk_user` in session/localStorage) into `FRONTEND/shared/session.js`, plus a role-aware `requireRole(role)` redirect helper. Consolidate the duplicated seed arrays (DUMMY_ACCOUNTS from login.js, the sponsor/beasiswa/pendaftaran arrays repeated in dashboard/pendaftaranSaya/daftarBeasiswa/etc.) into `FRONTEND/shared/dummyData.js` as a single seed source. Create `FRONTEND/shared/applicationRules.js` exposing the canonical snake_case status enum (KD-4: menunggu_verifikasi, lolos_berkas, ditolak_berkas, wawancara, lolos_final, tidak_lolos_final) with a display-label map, plus helpers: `isProfileComplete(profile)`, `areCoreDocsComplete(docs)`, `isReadyToApply(...)`, and `isApplicationEditable(application)` (editable only while status === 'menunggu_verifikasi' — this DEFINES D-10's cutoff).</action>
  <verify>grep -rni "bk_user\|menunggu_verifikasi\|isReadyToApply\|isApplicationEditable\|requireRole" FRONTEND/shared</verify>
  <acceptance_criteria>
    - `session.js` exposes getSession/saveSession/clearSession + requireRole, all on the existing `bk_user` contract
    - `dummyData.js` is the single seed source (no behavior change, just consolidation)
    - `applicationRules.js` defines the canonical status enum (KD-4) + a label map
    - `applicationRules.js` defines `isApplicationEditable` returning true only for status 'menunggu_verifikasi' (resolves D-10)
    - readiness helpers cover profile-complete AND core-docs-complete
  </acceptance_criteria>
  <done>Shared foundation exists; duplication is removed; status vocabulary is canonical.</done>
</task>

<task type="auto">
  <name>Task 2: Wire login to shared modules + real Supabase Auth (no routing regression)</name>
  <files>FRONTEND/LOGIN/login.js, FRONTEND/LOGIN/login.html</files>
  <read_first>FRONTEND/LOGIN/login.js, FRONTEND/LOGIN/login.html, FRONTEND/shared/supabaseConfig.js</read_first>
  <action>Update login.js to (1) import DUMMY_ACCOUNTS from `shared/dummyData.js` and session helpers from `shared/session.js` instead of owning private copies; (2) make the Supabase path primary: use `shared/supabaseConfig.js` for `signInWithPassword` and profile lookup (NIM→email already handled by nimToEmail), and gate the dummy-account branch behind an explicit `DEV_FALLBACK` flag with a console warning; (3) KEEP the existing ROLE_CONFIG and all four role redirects exactly as-is. Wire login.html to load `shared/supabaseConfig.js`, `shared/dummyData.js`, and `shared/session.js` before login.js (ordered classic scripts or type=module, consistent with how the page currently loads). Do NOT remove or rewrite the 4-role router.</action>
  <verify>grep -ni "DEV_FALLBACK\|supabaseConfig\|shared/session\|shared/dummyData" FRONTEND/LOGIN/login.js FRONTEND/LOGIN/login.html; grep -ni "dashboardStaffAdmin\|dashboardKabag\|dashboardWabag\|dashboard/dashboard" FRONTEND/LOGIN/login.js</verify>
  <acceptance_criteria>
    - login.js no longer owns the only copy of DUMMY_ACCOUNTS or session helpers
    - Supabase Auth is the primary path; dummy login is behind an explicit DEV_FALLBACK flag
    - all four role redirects (mahasiswa/staff/kabag/wabag) remain present and unchanged (regression guard)
    - login.html loads the shared config/session/data modules before login.js
  </acceptance_criteria>
  <done>Login uses the shared foundation and real auth without losing the working 4-role routing.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `grep -rn "bk_user\|menunggu_verifikasi\|isApplicationEditable" FRONTEND/shared FRONTEND/LOGIN/login.js` returns expected hits
- [ ] login.html loads shared modules before login.js
- [ ] Regression: logging in resolves each of the four roles to its existing dashboard
- [ ] Duplicated session/seed logic is removed from at least login.js (other pages migrate in 01-02/01-03)
- [ ] Dummy login only runs when DEV_FALLBACK is enabled; otherwise Supabase Auth is used
</verification>

<success_criteria>
- All tasks completed
- Shared session/seed/rules foundation exists and is consumed by login
- Real Supabase Auth is wired; 4-role routing preserved
- Canonical status vocabulary established for later slices
</success_criteria>

<output>
After completion, create `.planning/phases/01-intake-foundation-and-program-setup/01-01-SUMMARY.md`
</output>
