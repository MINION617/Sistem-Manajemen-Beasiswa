---
phase: 01-intake-foundation-and-program-setup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - FRONTEND/shared/session.js
  - FRONTEND/shared/dummyData.js
  - FRONTEND/shared/applicationRules.js
  - FRONTEND/shared/adminData.js
  - FRONTEND/LOGIN/login.js
  - FRONTEND/LOGIN/login.html
autonomous: true
requirements: [AUTH-01, PROF-01, DOCS-01, APPL-01, APPL-02, SPON-01, SCHL-01]
user_setup: []
must_haves:
  truths:
    - "Campus-provisioned users (D-04, D-05) can be resolved from shared dummy data and routed by role after login (D-02)."
    - "Scholarship, sponsor, profile, and application state are read from shared JavaScript modules instead of isolated page-local arrays, preserving the JS dummy-data-first direction (D-12)."
    - "Phase 1 pages consume one consistent application rule set for readiness and initial submission status so later slices do not re-implement the same decisions differently."
  artifacts:
    - path: "FRONTEND/shared/session.js"
      provides: "Shared session helpers and role-aware redirect logic"
      min_lines: 30
    - path: "FRONTEND/shared/dummyData.js"
      provides: "Normalized dummy source of truth for users, sponsors, scholarships, profiles, and applications"
      min_lines: 80
    - path: "FRONTEND/shared/applicationRules.js"
      provides: "Shared readiness and submission rule helpers"
      min_lines: 30
    - path: "FRONTEND/LOGIN/login.js"
      provides: "Login integration with shared dummy data and role routing"
      contains: "role"
    - path: "FRONTEND/LOGIN/login.html"
      provides: "Script wiring for shared dummy data/session modules used by login"
      contains: "script"
  key_links:
    - from: "FRONTEND/LOGIN/login.js"
      to: "FRONTEND/shared/dummyData.js"
      via: "shared user lookup"
      pattern: "dummyData|DUMMY|shared"
    - from: "FRONTEND/LOGIN/login.js"
      to: "FRONTEND/shared/session.js"
      via: "session persistence helpers"
      pattern: "sessionStorage|localStorage|saveSession"
    - from: "FRONTEND/shared/applicationRules.js"
      to: "FRONTEND/shared/dummyData.js"
      via: "readiness evaluation against shared profile/document state"
      pattern: "profile|document|application"
---

<objective>
Establish the Phase 1 walking-skeleton foundation for shared dummy data, role-aware session handling, and common application rules.

Purpose: Prevent the mahasiswa and admin slices from drifting apart by centralizing the JS dummy data (D-12) and the rules that gate profile readiness and application submission.
Output: Shared frontend modules for seed data, session handling, and application rules, plus a login flow updated to use them.
</objective>

<execution_context>
@$HOME/.codex/gsd-core/workflows/execute-plan.md
@$HOME/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-intake-foundation-and-program-setup/01-CONTEXT.md
@.planning/phases/01-intake-foundation-and-program-setup/01-RESEARCH.md
@FRONTEND/LOGIN/login.js
@FRONTEND/LOGIN/login.html
@FRONTEND/daftarBeasiswa/daftarBeasiswa.js
@FRONTEND/profilMahasiswa/profilMahasiswa.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create shared Phase 1 dummy domain modules</name>
  <files>FRONTEND/shared/dummyData.js, FRONTEND/shared/applicationRules.js, FRONTEND/shared/adminData.js</files>
  <read_first>FRONTEND/daftarBeasiswa/daftarBeasiswa.js, FRONTEND/profilMahasiswa/profilMahasiswa.js, BACKEND/supabaseclient.js</read_first>
  <action>Create shared JS modules that normalize the existing page-local dummy data into reusable collections and helper functions. Include campus-provisioned users with roles, mahasiswa profiles, standard document package requirements, sponsor records, scholarship programs, and application records with initial `menunggu_verifikasi` mapping. Add rule helpers that answer: whether a mahasiswa is profile-complete, whether core documents are complete, whether the student may submit, and whether a submitted application is still editable before verification starts. Keep shapes easy to map later onto the existing Supabase client naming.</action>
  <verify>rg -n "menunggu_verifikasi|canSubmit|documents|sponsors|scholarships" FRONTEND/shared</verify>
  <acceptance_criteria>
    - `FRONTEND/shared/dummyData.js` exists and contains normalized collections for users, sponsors, scholarships, profiles, and applications
    - `FRONTEND/shared/applicationRules.js` contains explicit readiness helpers for profile completion, document completion, and editability before verification
    - `FRONTEND/shared/adminData.js` exposes sponsor/program CRUD-oriented helper functions over the shared dummy state
  </acceptance_criteria>
  <done>Shared Phase 1 dummy data and rule helpers exist and are reusable from multiple pages.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor login to use shared accounts and role redirects</name>
  <files>FRONTEND/LOGIN/login.js, FRONTEND/LOGIN/login.html, FRONTEND/shared/session.js</files>
  <read_first>FRONTEND/LOGIN/login.js, FRONTEND/LOGIN/login.html, FRONTEND/dashboard/dashboard.html, FRONTEND/SPONSOR/sponsor.html</read_first>
  <action>Extract session persistence and role-aware redirect helpers into `FRONTEND/shared/session.js`, then update `FRONTEND/LOGIN/login.js` to authenticate against the shared campus-provisioned dummy account source instead of page-local account definitions. Wire `FRONTEND/LOGIN/login.html` so the browser actually loads the shared dummy data and session dependencies in the chosen pattern: either convert the login script to `type="module"` with import-compatible paths, or add ordered shared script tags before `login.js` if the repo keeps classic scripts. Preserve NIM-based login and reset assumptions from the context file. After successful login, route mahasiswa to the student flow and admins to the minimal internal sponsor/program surface. Do not add self-registration as a first-class Phase 1 path.</action>
  <verify>rg -n "role|nim|saveSession|redirect" FRONTEND/LOGIN/login.js FRONTEND/shared/session.js; rg -n "type=\"module\"|shared/dummyData|shared/session|login.js" FRONTEND/LOGIN/login.html</verify>
  <acceptance_criteria>
    - `FRONTEND/LOGIN/login.js` no longer owns the only copy of dummy account data
    - `FRONTEND/LOGIN/login.html` loads the shared modules or ordered shared scripts needed by the refactored login code
    - Login flow contains explicit role-based routing for mahasiswa and admin
    - `FRONTEND/shared/session.js` provides shared session helpers consumed by login and reusable by later pages
  </acceptance_criteria>
  <done>Shared login/session foundation is in place and aligns with the campus-provisioned account decision.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `rg -n "role|menunggu_verifikasi|canSubmit|canEdit" FRONTEND/shared FRONTEND/LOGIN/login.js` returns expected hits
- [ ] `FRONTEND/LOGIN/login.html` is wired so shared data/session code runs before or inside the login flow
- [ ] Browser smoke check: log in once as a mahasiswa dummy account and once as an admin dummy account, confirming each reaches the correct role-specific page
- [ ] Shared modules remove the need for page-local duplication of core dummy domain data
- [ ] Login routing clearly separates mahasiswa and admin entry after authentication
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Shared dummy data, session logic, and submission rules exist as the single Phase 1 foundation for later plans
</success_criteria>

<output>
After completion, create `.planning/phases/01-intake-foundation-and-program-setup/01-01-SUMMARY.md`
</output>
