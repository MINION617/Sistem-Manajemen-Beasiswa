---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [
  "docker-compose.yml",
  "nginx/nginx.conf",
  "backend/Dockerfile",
  "frontend/Dockerfile",
  "backend/.sequelizerc",
  "backend/config/config.json",
  "backend/migrations/20240612000001-create-users.js",
  "backend/migrations/20240612000002-create-sponsors.js",
  "backend/migrations/20240612000003-create-scholarships.js"
]
autonomous: true
requirements: [FR-007]

must_haves:
  truths:
    - "Docker containers for frontend, backend, mysql, and nginx start successfully"
    - "MySQL schema is initialized with users, sponsors, and scholarships tables"
  artifacts:
    - path: "docker-compose.yml"
      provides: "Container orchestration"
    - path: "backend/migrations/20240612000002-create-sponsors.js"
      provides: "Sponsor table definition"
  key_links:
    - from: "backend"
      to: "mysql"
      via: "Sequelize connection"
---

<objective>
Setup the project's containerized infrastructure and initialize the core database schema.

Purpose: Provide a stable development environment and persistent data layer.
Output: Working Docker Compose environment and Sequelize migration history.
</objective>

<execution_context>
@$HOME/.gemini/gsd-core/workflows/execute-plan.md
@$HOME/.gemini/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/research/STACK.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Docker Infrastructure Setup</name>
  <files>docker-compose.yml, nginx/nginx.conf, backend/Dockerfile, frontend/Dockerfile</files>
  <action>
    Create a docker-compose.yml defining 4 services:
    - mysql:8.0 with persistent volume and SIM_BEASISWA_DB.
    - backend: Node.js (Alpine) with Express boilerplate.
    - frontend: React (Vite) with Nginx for production-like serving.
    - nginx: Reverse proxy routing /api to backend and / to frontend.
    Ensure .env file is used for secrets (DB_PASS, SUPABASE_URL, etc.).
  </action>
  <verify>
    <automated>docker compose up -d && docker compose ps --format json | grep "running"</automated>
  </verify>
  <done>All 4 containers are in 'running' state and accessible via localhost.</done>
</task>

<task type="auto">
  <name>Task 2: Sequelize Initialization & Core Migrations</name>
  <files>backend/.sequelizerc, backend/config/config.json, backend/migrations/*.js</files>
  <action>
    Initialize Sequelize in the backend directory.
    Create migrations for the following tables (per D-01):
    1. users: id (UUID), email, role (enum: MAHASISWA, STAFF, KABAG, KEUANGAN), supabase_id (UUID, unique).
    2. sponsors: id, name, description, contact_info.
    3. scholarships: id, sponsor_id (FK), name, description, amount, deadline, status (enum: OPEN, CLOSED).
    Ensure unique constraints on email and sponsor names.
  </action>
  <verify>
    <automated>docker compose exec backend npx sequelize-cli db:migrate</automated>
  </verify>
  <done>MySQL tables are created successfully and verified via 'SHOW TABLES;' in mysql container.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Network | External access restricted to Nginx (ports 80/443) |
| Internal | Backend to MySQL communication within Docker network |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information Disclosure | .env files | mitigate | Add .env to .gitignore and use Docker secrets/env files |
| T-01-SC | Tampering | npm installs | mitigate | slopcheck + blocking human checkpoint for [ASSUMED] packages |
</threat_model>

<verification>
Verify that 'docker compose up' works and 'npx sequelize-cli db:migrate' completes without errors.
</verification>

<success_criteria>
- Frontend accessible at http://localhost
- Backend API root returns 200 OK
- Database tables exist in MySQL
</success_criteria>

<output>
Create `.planning/phases/01-foundation/01-01-SUMMARY.md` when done
</output>
