---
phase: 01-foundation
plan: 03
type: execute
wave: 3
depends_on: ["01-02"]
files_modified: [
  "backend/models/index.js",
  "backend/models/sponsor.js",
  "backend/models/scholarship.js",
  "backend/controllers/sponsorController.js",
  "backend/controllers/scholarshipController.js",
  "backend/routes/adminRoutes.js",
  "frontend/src/pages/admin/SponsorManagement.jsx",
  "frontend/src/pages/admin/ScholarshipManagement.jsx"
]
autonomous: true
requirements: [FR-007]

must_haves:
  truths:
    - "Staff can create, read, update, and delete sponsor records"
    - "Staff can manage scholarship programs linked to sponsors"
  artifacts:
    - path: "backend/controllers/sponsorController.js"
      provides: "CRUD logic for sponsors"
    - path: "frontend/src/pages/admin/SponsorManagement.jsx"
      provides: "UI for sponsor management"
  key_links:
    - from: "backend/models/scholarship.js"
      to: "backend/models/sponsor.js"
      via: "belongsTo (sponsor_id)"
---

<objective>
Implement CRUD modules for managing Sponsors and Scholarship programs.

Purpose: Allow Staff to populate the system with master data for scholarships.
Output: Backend API endpoints and Frontend administrative dashboards for Sponsors and Scholarships.
</objective>

<execution_context>
@$HOME/.gemini/gsd-core/workflows/execute-plan.md
@$HOME/.gemini/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/phase-1/01-02-SUMMARY.md
@backend/migrations/20240612000002-create-sponsors.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sponsor CRUD (Backend & Frontend)</name>
  <files>backend/controllers/sponsorController.js, backend/routes/adminRoutes.js, frontend/src/pages/admin/SponsorManagement.jsx</files>
  <action>
    Backend:
    - Create Express routes and controllers for GET, POST, PUT, DELETE /api/admin/sponsors.
    - Protect routes using `authMiddleware` and verify `req.user.role === 'STAFF'`.
    Frontend:
    - Create a page with a table showing sponsors.
    - Add a form (modal) to Create/Edit sponsors using Axios interceptor client.
  </action>
  <verify>
    <automated>curl -X GET http://localhost/api/admin/sponsors (Expect 401 if no token)</automated>
  </verify>
  <done>Staff can perform full CRUD operations on Sponsor data via the web UI.</done>
</task>

<task type="auto">
  <name>Task 2: Scholarship CRUD (Backend & Frontend)</name>
  <files>backend/controllers/scholarshipController.js, backend/models/scholarship.js, frontend/src/pages/admin/ScholarshipManagement.jsx</files>
  <action>
    Backend:
    - Implement GET, POST, PUT, DELETE /api/admin/scholarships.
    - Ensure `sponsor_id` is validated against existing sponsors.
    Frontend:
    - Create a page to manage scholarship programs.
    - Include fields for amount, deadline, and a dropdown for selecting the sponsor.
  </action>
  <verify>
    <automated>npm test -- backend/tests/scholarship.test.js (if test exists)</automated>
  </verify>
  <done>Staff can manage active scholarship programs and link them to sponsors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin API | Access restricted to 'STAFF' role |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04 | Tampering | CRUD Endpoints | mitigate | Enforce RBAC (Role-Based Access Control) in authMiddleware and controllers |
| T-01-05 | Information Disclosure | Database IDs | mitigate | Use UUIDs for IDs (already in migrations) to prevent enumeration |
</threat_model>

<verification>
Create a test sponsor and scholarship in the UI, verify they appear in the table, edit them, and delete them.
</verification>

<success_criteria>
- Only users with role 'STAFF' can access /api/admin/* routes.
- Data persists in MySQL correctly.
- Unique constraints prevent duplicate sponsors with the same name.
</success_criteria>

<output>
Create `.planning/phases/01-foundation/01-03-SUMMARY.md` when done
</output>
