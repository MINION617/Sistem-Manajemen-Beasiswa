---
phase: 01-foundation
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified: [
  "backend/middleware/auth.js",
  "backend/routes/auth.js",
  "frontend/src/api/client.js",
  "frontend/src/contexts/AuthContext.jsx",
  "frontend/src/pages/Login.jsx",
  "frontend/src/App.jsx"
]
autonomous: true
requirements: [FR-001]

must_haves:
  truths:
    - "Users can log in via Frontend and access role-protected pages"
    - "Backend rejects requests with invalid or missing Supabase JWT"
  artifacts:
    - path: "backend/middleware/auth.js"
      provides: "JWT verification via Supabase Admin SDK"
    - path: "frontend/src/api/client.js"
      provides: "Axios instance with JWT interceptors"
  key_links:
    - from: "frontend/src/api/client.js"
      to: "backend/middleware/auth.js"
      via: "Authorization: Bearer <token> header"
---

<objective>
Implement the authentication flow using Supabase Auth and role-based access control.

Purpose: Secure the system and distinguish between Mahasiswa, Staff, Kabag, and Keuangan roles.
Output: Auth middleware, central Axios client, and a functional login page.
</objective>

<execution_context>
@$HOME/.gemini/gsd-core/workflows/execute-plan.md
@$HOME/.gemini/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/phase-1/01-01-SUMMARY.md
@.planning/phases/phase-1/01-DISCUSSION-LOG.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Backend Auth Middleware (Supabase Admin SDK)</name>
  <files>backend/middleware/auth.js, backend/routes/auth.js</files>
  <action>
    Implement auth middleware using `@supabase/supabase-js` (Admin SDK) as per D-02.
    - Extract JWT from Bearer header.
    - Verify JWT using `supabase.auth.getUser(token)`.
    - If valid, find the user in MySQL by `supabase_id` and attach the MySQL user object (including role) to `req.user`.
    - Create a test route `/api/auth/me` to return current user info.
  </action>
  <verify>
    <automated>curl -H "Authorization: Bearer INVALID" http://localhost/api/auth/me | grep 401</automated>
  </verify>
  <done>Middleware correctly validates Supabase JWTs and populates req.user from MySQL.</done>
</task>

<task type="auto">
  <name>Task 2: Frontend Auth Logic (Axios & Context)</name>
  <files>frontend/src/api/client.js, frontend/src/contexts/AuthContext.jsx</files>
  <action>
    Configure Central Axios Client as per D-03:
    - Add request interceptor to inject `Authorization: Bearer <token>` from Supabase session.
    - Add response interceptor to handle 401/403 errors (logout user).
    Implement AuthContext to manage Supabase session state and provide `user`, `role`, and `login/logout` functions to the app.
  </action>
  <verify>
    <automated>grep -r "axios.create" frontend/src/api/client.js && grep "interceptors.request.use" frontend/src/api/client.js</automated>
  </verify>
  <done>Axios automatically includes tokens, and AuthContext tracks user session state.</done>
</task>

<task type="auto">
  <name>Task 3: Login Page and Routing</name>
  <files>frontend/src/pages/Login.jsx, frontend/src/App.jsx</files>
  <action>
    Create a Login page using Tailwind CSS:
    - Form for Email/NIM and Password.
    - Handle Supabase `signInWithPassword`.
    - On success, redirect to dashboard based on role.
    Update `App.jsx` with ProtectedRoutes that check the user's role.
  </action>
  <verify>
    <automated>npm test -- frontend/src/pages/Login.test.jsx (if test exists) or manual verification</automated>
  </verify>
  <done>Users can log in and are redirected to their respective role-based dashboards.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client-Server | Untrusted JWT from client must be verified by Supabase |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-02 | Spoofing | Auth Header | mitigate | Verify token validity via Supabase Admin SDK on every request |
| T-01-03 | Elevation of Privilege | User Roles | mitigate | Fetch role from MySQL (backend) based on verified Supabase ID |
</threat_model>

<verification>
Manual login with a test Supabase account and checking the '/api/auth/me' response in the browser console.
</verification>

<success_criteria>
- Unauthenticated requests to /api/auth/me return 401.
- Authenticated requests return user data including role.
- Frontend redirects unauthenticated users to /login.
</success_criteria>

<output>
Create `.planning/phases/01-foundation/01-02-SUMMARY.md` when done
</output>
